var Napkin = Napkin || {};

(function(N){

    if (!N.Class) return;

    var select = function(selector, ctx){
        var i, l;
        if (typeof selector !== 'string') throw Error('Napkin.select(): Selector must be a string');
        selector = selector.split(' ');
        for (i = 0, l < selector.length; i < l; i++){
            
        }
        ctx = ctx || Document;
    }

})(Napkin);